spring:
  application:
    name: spring-jdbc-template
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/test?useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver